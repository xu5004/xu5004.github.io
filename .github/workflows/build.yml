
name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Install Quarto
      - uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: "1.6.40" # Pin version or omit for latest

      # Verify installation
      - run: quarto --version

      # Your build steps here
      - run: quarto render
``
